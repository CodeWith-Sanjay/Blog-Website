<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Website</title>
    <link rel="stylesheet" href="./styles/main.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css">
</head>
<body class="body-container d-flex justify-content-center align-items-center">
    
    <div class="container blog-container" >

        <h3 class="p-2">All blog posts</h3>
        <% posts.forEach((post, index) => {%>
        <div class="row p-3">
            <h3><%= post.title %></h3>
            <p><%= post.content %></p>
            <p><%= post.author %></p>
            <p><%= post.date.toDateString() %></p>

            <a href="/post" class="col-12 col-sm-6 d-flex justify-content-center align-items-center form-btn">POST</a>
            <a href="/edit/<%= index %>" class="col-12 col-sm-6 d-flex justify-content-center align-items-center form-btn">EDIT</a>          

        </div>
        <% }) %>
        
    </div>

</body>
</html>